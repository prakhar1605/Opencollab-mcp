# GitHub personal access token (classic or fine-grained)
# Scopes needed: public_repo (read-only access to public repositories is enough)
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Transport: "stdio" (default, local) or "streamable-http" (remote)
# TRANSPORT=stdio

# Port for HTTP transport (only used when TRANSPORT=streamable-http)
# PORT=8000

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# OPENCOLLAB_LOG_LEVEL=INFO
